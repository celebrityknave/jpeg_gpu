// This will ultimately be the main interface which can ultimately call the encoder-decoder and random image generation capabilities. Obviously WIP.

#include <stdio.h>
#include <stdlib.h>

int main(int, char**){
    printf("Hello, from jpeg_gpu_decoder!\n");
}
