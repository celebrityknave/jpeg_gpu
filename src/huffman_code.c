#include <stdio.h>
#include <string.h>

typedef struct data_node {
    char data;
    unsigned freq;
} data_node;

data_node getFrequencyTable(char* input_data)
{
    // Populate data categories
    for(int i=0; i < strlen(input_data); i++)
    {

    } 
}


int main()
{
    char* arr = "This is an example of a Huffman tree";

}

